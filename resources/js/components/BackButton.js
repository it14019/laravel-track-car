import React from 'react';

const BackButton = () => {
    return (
        <div className='back-button'>
            <a href={'/'}>
                <button>back</button>
            </a>
        </div>
    );
};

export default BackButton;
